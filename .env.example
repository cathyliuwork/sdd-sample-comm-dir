# Supabase Database Connection
# Transaction mode (for Prisma Client queries) - port 6543
DATABASE_URL="postgresql://postgres.[REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Session mode (for Prisma Migrate) - port 5432
DIRECT_URL="postgresql://postgres.[REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Application Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# JWT Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-chars"
JWT_EXPIRES_IN="7d"

# Admin Credentials
# Generate hash with: npx tsx scripts/generate-password-hash.ts YourPassword123
# IMPORTANT: Escape $ signs with backslash in .env files
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH="\$2b\$10\$..."

# ==================================================
# Deployment Configuration (AI Builder/Koyeb)
# ==================================================

# PORT Environment Variable
# - Automatically set by Koyeb at runtime
# - DO NOT set PORT manually in deployment environment
# - For local development, PORT defaults to 3000
# PORT=3000

# AI_BUILDER_TOKEN (optional, auto-injected by platform)
# - Automatically provided by AI Builder platform
# - Can be used to call AI Builder backend services
# - This project does not require it (uses Supabase only)
# AI_BUILDER_TOKEN="auto-injected-by-platform"

# Production Environment Variables
# - Set these in AI Builder deployment API
# - Never commit real values to Git repository
# - Use strong secrets in production:
#   * JWT_SECRET: Generate with crypto.randomBytes (min 32 chars)
#   * ADMIN_PASSWORD: Strong password (min 12 chars)
#   * DATABASE_URL: From Supabase Dashboard
